# Interface Rhythm Notes (Prompt Field Anchoring + Tool Rhythm Pass)

## Intent

Reduce detached-object feeling and move toward one authored instrument field across:

- prompt zone
- utility controls
- account control
- Patterns activation

## Account Control Rhythm

- Removed contour/drop-shadow treatment from account glyph.
- Kept quiet linework only.
- Preserved top-right infrastructural placement.
- Kept behavior and accessibility wiring unchanged.

Result: account control reads as infrastructural signal, not mobile app chrome.

## Patterns Activation Rhythm (kept as “Patterns”)

- Replaced manila-tab/folder treatment with restrained line-state language.
- Patterns now uses:
  - quiet uppercase label treatment
  - latent top-edge state marker (`::before`)
  - hover/focus faint top-line preview
  - active firm top-line state only when expanded (`aria-expanded="true"`).

Result: Patterns reads as systemic activation state, not filing-tab UI.

## Prompt Field Anchoring (regression-corrected)

- Kept single shared prompt field grid and corrected over-compression regressions:
  - utility lane widened back to stable visibility width
  - control stack compactness preserved without clipping
  - reduced dead space between controls while keeping icon integrity
  - controls remain anchored to prompt reading-origin/top-line rhythm rather than card-center drift
- Preserved existing control affordances and IDs.
- Tightened prompt vertical inset and control gap to unify cadence with surrounding rules.

Result: prompt text and utility controls now read as one authored instrument strip across short and multi-line prompts.

## Tool Family Unification

- Unified infrastructural tool quietness through shared opacity rhythm tokens:
  - rest / hover / active
- Added shared tool color token so passive icons use one gray family at rest.
- Applied consistency across:
  - account icon
  - Patterns trigger state behavior
  - prompt fold/unfold glyph
  - prompt reroll glyph
  - gear icon

Result: tools now behave like one latent infrastructural layer instead of mixed chrome intensity or uneven blackness.

## Right Rail Alignment

- Corrected account icon rail position so the visible glyph edge sits closer to PATTERNS terminal right edge.
- Preserved quiet top-right infrastructural posture.

## Logo Load Polish

- Added preload for the Wayword logo SVG asset in initial HTML head.
- Goal: reduce delayed mask-asset pop-in on landing/header load and make brand mark appear immediately.

## Anti-Drift Guardrails Preserved

- no auth logic changes
- no Patterns runtime logic changes
- no feature expansion
- account icon remains shadowless and quiet

## Right Rail + Logo Tone Regression Fix

- Corrected top-right optical rail so PATTERNS and Account share the same authored edge treatment.
- Shifted account glyph optical position to align visible icon geometry (not just its button box) with the shared right rail.
- Preserved no-tab/non-skeuomorphic PATTERNS treatment and quiet account icon behavior.
- Made logo tone theme-token controlled so it no longer renders as raw black in light mode and remains legible in dark mode.
- Kept logo geometry unchanged and retained immediate-load behavior.

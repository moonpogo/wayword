/**
 * Template strings / pattern tables for deterministic reflection copy.
 * Populated when generation rules land.
 */
export const MIRROR_REFLECTION_TEMPLATES: Readonly<Record<string, never>> = Object.freeze({});

import type { MirrorReflection } from "../types/mirrorTypes.js";

/** Placeholder: collapse near-duplicate reflections by evidence overlap. */
export function dedupeReflections(reflections: MirrorReflection[]): MirrorReflection[] {
  return reflections;
}

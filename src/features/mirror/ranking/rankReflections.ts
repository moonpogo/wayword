import type { MirrorFeatures, MirrorReflection } from "../types/mirrorTypes.js";

/** Placeholder: order and cap candidates from extracted features. */
export function rankReflections(features: MirrorFeatures): MirrorReflection[] {
  void features;
  return [];
}
